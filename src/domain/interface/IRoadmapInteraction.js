import { IInteraction } from './IInteraction';

/**
 * @interface IRoadmapInteraction
 * @extends IInteraction
 */
export class IRoadmapInteraction extends IInteraction {
    // Specific interaction methods for roadmaps can be added here
}
